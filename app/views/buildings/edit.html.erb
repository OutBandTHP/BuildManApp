<% provide(:title, 'Update Building') %>
<% provide(:button_text, 'Submit Changes') %>

<h1>Update Building <%= @building.name %> of <%= @building.project.name %></h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
  </div>
</div>
