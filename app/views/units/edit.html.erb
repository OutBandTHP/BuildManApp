<% provide(:title, 'Update Unit') %>
<% provide(:button_text, 'Submit Changes') %>

<h1>Update Unit <%= @unit.unit_no %> of <%= @unit.project.name %></h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
  </div>
</div>
