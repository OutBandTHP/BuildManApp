<% provide(:title, t('sessions.update_user')) %>
<% provide(:button_text, t('sessions.save')) %>
<h1><%= t('sessions.update_user') %></h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
    <div class="gravatar_edit">
      <%= gravatar_for @user %>
      <a href="https://gravatar.com/emails" target="_blank">
        <%= t('sessions.change') %>
      </a>
    </div>
  </div>
</div>