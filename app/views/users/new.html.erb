<% provide(:title, t('sessions.signup')) %>
<% provide(:button_text, t('sessions.create_acct')) %>
<h1><%= t('sessions.signup') %></h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
  </div>
</div>